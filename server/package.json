{
  "name": "tagteam2-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "node --watch --env-file=.env src/index.mjs",
    "start": "node --env-file=.env src/index.mjs",
    "test": "node --test src/*.test.mjs"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
